/**
 * @deprecated 請使用 useAuth from '@/components/providers/AuthProvider'
 * 此 hook 保留僅為向後兼容，實際邏輯已移至 AuthProvider
 */
export { useAuth as useSupabaseAuth, type UserRole } from '@/components/providers/AuthProvider';
